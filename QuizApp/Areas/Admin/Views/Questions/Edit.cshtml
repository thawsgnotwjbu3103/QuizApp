@model QuizApp.Models.Question

@{
    ViewData["Title"] = "Tạo câu hỏi";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">@ViewBag.Title</h6>
    </div>
    <div class="card-body">
        <form asp-area="Admin" asp-controller="Questions" asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="QuestionId" />
            <div class="row">
                <div class="form-group col-6">
                    <label asp-for="QuestionTitle" class="control-label">Tên câu hỏi</label>
                    <textarea asp-for="QuestionTitle" rows="6" class="form-control" ></textarea>
                    <span asp-validation-for="QuestionTitle" class="text-danger"></span>
                </div>
                <div class="form-group form-check col-6">
                    <label class="form-check-label">
                        <input class="form-check-input" asp-for="IsMultipleChoices" />Nhiều lựa chọn
                    </label>
                </div>
                <div class="form-group col-12">
                    <div class="float-right">
                        <input type="submit" value="Lưu" class="btn btn-primary" />
                        <a asp-area="Admin" asp-controller="Questions" asp-action="Index" asp-route-id="@ViewBag.qId" class="btn btn-outline-primary">Hủy</a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
